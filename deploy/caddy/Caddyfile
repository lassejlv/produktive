{
	email {$ACME_EMAIL}
	on_demand_tls {
		ask {$PRODUKTIVE_ORIGIN}/api/public/custom-domains/authorize
	}
}

:80 {
	redir https://{host}{uri} permanent
}

https:// {
	tls {
		on_demand
	}

	reverse_proxy {$PRODUKTIVE_ORIGIN} {
		header_up Host produktive.app
		header_up X-Forwarded-Proto https
	}
}
