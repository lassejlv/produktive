/** Log ingest, search, and storage are enabled on this deployment. */
export const LOGS_ENABLED = true;
/** Docker deployments private preview entry points are visible in this build. */
export const DEPLOYMENTS_ENABLED = true;
/** Sprites.dev sandboxes under Deployments are visible in this build. */
export const SANDBOXES_ENABLED = true;
