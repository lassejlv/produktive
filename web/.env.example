# Local dev uses the Vite /api proxy by default.
# Set this only when the browser should call a separate API origin directly.
# VITE_API_URL="http://localhost:3000"
# VITE_API_PROXY_TARGET="http://localhost:3000"
