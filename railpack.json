{
  "$schema": "https://schema.railpack.com",
  "packages": {
    "bun": "latest",
    "rust": "1.95.0"
  },
  "steps": {
    "build": {
      "inputs": [
        "."
      ],
      "commands": [
        "cargo build --release -p produktive-api",
        "cd web && bun install --frozen-lockfile",
        "cd web && bun run build"
      ]
    }
  },
  "deploy": {
    "aptPackages": [
      "ca-certificates"
    ],
    "inputs": [
      {
        "step": "build",
        "include": [
          "target/release/produktive-api",
          "web/dist/**"
        ]
      }
    ],
    "startCommand": "./target/release/produktive-api"
  }
}
